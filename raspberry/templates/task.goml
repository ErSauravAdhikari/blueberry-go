<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Raspberry - Task Scheduler</title>
    <link href="https://cdn.jsdelivr.net/npm/flowbite@2.4.1/dist/flowbite.min.css" rel="stylesheet"/>
    <script>
        // On page load or when changing themes, best to add inline in `head` to avoid FOUC
        if (localStorage.getItem('color-theme') === 'dark' || (!('color-theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
        } else {
            document.documentElement.classList.remove('dark');
        }
    </script>
</head>
<body class="bg-gray-100 text-gray-900 dark:bg-gray-800 dark:text-gray-100">
{{ template "navbar.goml" . }}
<div class="container mx-auto p-4">
    <h1 class="text-3xl font-bold mb-4">Task: {{.TaskName}}</h1>
    <a href="/task/{{.TaskName}}/run" class="text-white bg-blue-500 hover:bg-blue-700 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center inline-block">Run</a>
    <h2 class="text-2xl font-semibold mb-2">Schedules</h2>
    <div class="relative overflow-x-auto shadow-md sm:rounded-lg">
        <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
            <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
            <tr>
                <th scope="col" class="px-6 py-3">Schedule</th>
                <th scope="col" class="px-6 py-3">Next Scheduled At</th>
            </tr>
            </thead>
            <tbody>
            {{range .Schedules}}
                <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700">
                    <td class="px-6 py-4">{{.Schedule}}</td>
                    <td class="px-6 py-4">{{.NextExecution}}</td>
                </tr>
            {{end}}
            </tbody>
        </table>
    </div>
    <h2 class="text-2xl font-semibold mt-4 mb-2">Past Executions</h2>
    <div class="relative overflow-x-auto shadow-md sm:rounded-lg">
        <table class="w-full text-sm text-left rtl:text-right text-gray-500 dark:text-gray-400">
            <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
            <tr>
                <th scope="col" class="px-6 py-3">Start Time</th>
                <th scope="col" class="px-6 py-3">End Time</th>
                <th scope="col" class="px-6 py-3">Status</th>
            </tr>
            </thead>
            <tbody>
            {{range .Executions}}
                <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700">
                    <td class="px-6 py-4">
                        <a href="/execution/{{.ID}}" class="text-blue-500 hover:underline">{{.StartTime}}</a>
                    </td>
                    <td class="px-6 py-4">{{.EndTime}}</td>
                    <td class="px-6 py-4">
                                <span class="{{if eq .Status "completed"}}text-green-500 dark:text-green-400{{else if eq .Status "failed"}}text-red-500 dark:text-red-400{{else}}text-gray-700 dark:text-gray-300{{end}}">
                                    {{.Status}}
                                </span>
                    </td>
                </tr>
            {{end}}
            </tbody>
        </table>
    </div>
    <a href="/" class="text-blue-500 hover:underline mt-4 inline-block">Back to all tasks</a>
</div>
{{ template "scripts.goml" . }}
</body>
</html>
