<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{.TaskName}} - Raspberry</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900">
    <div class="container mx-auto p-4">
        <h1 class="text-3xl font-bold mb-4">Task: {{.TaskName}}</h1>
        <h2 class="text-2xl font-semibold mb-2">Schedules</h2>
        <ul>
            {{range .Schedules}}
            <li class="mb-2">
                <span class="text-sm text-gray-600">Schedule: {{.Schedule}}</span><br>
                <span class="text-sm text-gray-600">Next Scheduled At: {{.NextExecution}}</span>
            </li>
            {{end}}
        </ul>
        <h2 class="text-2xl font-semibold mt-4 mb-2">Past Executions</h2>
        <ul>
            {{range .Executions}}
            <li class="mb-2">
                <a href="/execution/{{.ID}}" class="text-blue-500 hover:underline">{{.StartTime}} - {{.Status}}</a>
            </li>
            {{end}}
        </ul>
        <a href="/" class="text-blue-500 hover:underline">Back to all tasks</a>
    </div>
</body>
</html>
